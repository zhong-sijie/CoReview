/**
 * 主页底部组件
 *
 * 提供主页的底部区域，包含分隔线和基础样式。
 * 使用VS Code主题变量确保与编辑器主题保持一致。
 */
const HomeFooter = () => {
  return (
    <footer className="border-t border-[var(--vscode-panel-border)] px-4 py-2" />
  );
};

export default HomeFooter;
